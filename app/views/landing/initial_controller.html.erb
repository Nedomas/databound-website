# You can create your own controller with permissions
class UsersController < ApplicationController
  databound do
    model :user
    columns :name, :email, :user_id

    permit(:update, :destroy) do |params, record|
      record.user_id == current_user.id
    end
  end
end
